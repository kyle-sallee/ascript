By   ko
the  words      kernel object       is      abbreviated.
Like ascript
the  Linux      kernel also         is      modular.
By   ascript    kernel objects can  be      located.
By   ascript    kernel objects can  be      loaded.
Cold plug,
hot  plug,      and
warm plug       events         can  be      handled.

The  Linux      kernel
if   not        used
then ascript    compilation    can  fail
yet  these      argot               are not desired.

For  unsigned   modules        load is      supported.
For    signed   modules        load is      supported.
For  compressed module         load is  not supported.

From the        software       project      kmod
the  library    and
the  programs                       are not used.
